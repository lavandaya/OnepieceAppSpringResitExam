<!DOCTYPE html>
<html lang="en" xmlns:th="http://www.thymeleaf.org">
<body>
<nav th:fragment="navbar">
    <a th:href="@{/characters}" th:text="#{nav.characters}">Character List</a> |
    <a th:href="@{/battles}" th:text="#{nav.battles}">Battle List</a> |
    <a th:href="@{/characters/add}" th:text="#{nav.addCharacter}">Add Character</a> |
    <a th:href="@{/battles/add}" th:text="#{nav.addBattle}">Add Battle</a>
</nav>
</body>
</html>
